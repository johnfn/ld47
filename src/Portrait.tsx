import React, { useEffect } from 'react';


export const Portrait = ({ fileName }: { fileName: string }) => {
  const [image, setImage] = React.useState();

  return (<div></div>);

  //  <img src={require(`images/${fileName}.png`)} alt="product" />

  //);
}